import React from 'react';
import LoginForm from './LoginForm'

const Login = () => {
    return (
        <main>
            <section className='page'>
                {<LoginForm />}
            </section>
        </main>
    )
}

export default Login;